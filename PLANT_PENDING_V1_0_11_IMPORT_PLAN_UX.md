# Plant Pending v1.0.11 Import plan UX

Clarifies browser-saved plans vs imported JSON plans.

Changes:
- Renames the modal to "Open browser plan"
- Explains that local saves are per browser/domain
- Adds "Import plan JSON" directly inside the open-plan modal
- Adds "Load example plan" directly inside the open-plan modal
- Importing a JSON closes the modal and saves the imported plan into this browser
