# Plant Pending v1.0.6 GitHub Pages full path fix

Fixes remaining GitHub Pages asset path issues:
- Restores Vite `base: '/plant-pending/'`
- Uses `%BASE_URL%` for favicon in `index.html`
- Uses Vite `BASE_URL` for local plant thumbnails from `public/GreenAcres-Images`
- Keeps v1.0.4 CSV/JSON path fixes
- Keeps v1.0.5 rock/top-down SVG path fixes
