# Plant Pending v1.0 branding update

## Changed

- Header app name changed from Garden Planner to Plant Pending.
- Header version changed to v 1.0.
- Header subtitle changed to: Probably needs a shrub.
- Replaced the checkmark app mark with the uploaded Plant Pending app icon.
- Added uploaded brand assets to `public/brand/`:
  - `app-icon.svg`
  - `app-icon-mark.svg`
  - `logo.svg`
- Browser title and favicon updated to Plant Pending.
- Package version set to 1.0.0.

## Test

Run:

```powershell
npm install
npm run dev
```
