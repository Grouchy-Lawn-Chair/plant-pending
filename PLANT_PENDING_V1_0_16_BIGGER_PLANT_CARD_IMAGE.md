# Plant Pending v1.0.16 – Bigger plant card image

Changed:
- Increased the normal plant card image height from `h-28` to `h-36`.
- Increased the selected plant card image height from `h-36` to `h-44`.
- Slightly tightened the spacing between image and metric tiles.

Build check:
- npm run build passed.
