# Plant Pending v1.0.18 – Updated example plan

Changed:
- Replaced `public/examples/example-plan.json` with the newly uploaded example plan.
- Added `restoreBackgroundOnLaunch: true` so the example plan background image persists after refresh.

Notes:
- Keeps v1.0.17 card, zone, rock, density, and generator polish.
